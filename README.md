## Development

Frontend
`yarn dev-client`

Backend
`yarn dev-server` 

Database Locally:
`brew services start mongodb/brew/mongodb-community`

Heroku Local Test

`tsc` 

`yarn run build`

`heroku local web`